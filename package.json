{
  "name": "max_flow",
  "version": "1.0.0",
  "description": "",
  "main": "dist/vertex.js",
  "scripts": {
    "prebuild": "rm -rf dist && mkdir dist",
    "build": "babel src/index.js -o dist/index.js",
    "test": "mocha src/index.test.js -w --compilers js:babel-register",
    "test:single": "istanbul cover -x *.test.js _mocha -- -R spec src/lib/vertex.test.js --compilers js:babel-register"
  },
  "author": "Jerridan Quiring",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "chai": "^3.5.0",
    "istanbul": "^0.4.3",
    "mocha": "^2.4.5"
  }
}
